enum Labels {
  Player
  Coach
  Team
  Match
  League
  Season
  Stadium
  Referee
  Goal
}

enum Relations {
  BELONGS_TO
  COACHES
}

scalar JSON

type TResponse {
  statusCode: Int!
  message: String!
  data: JSON
}
